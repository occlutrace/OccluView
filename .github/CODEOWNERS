# One maintainer, everything. The five per-path lines that used to follow all
# named the same owner as the line above, which reads as a review policy for a
# team that does not exist. Paths come back when a second reviewer does.
* @occlutrace/maintainers
